#include <bits/stdc++.h>
using namespace std;

void solve() 
{
    int p;
    cin >> p;
    cout << 2 << " " << p - 1 << endl;
}

int main() 
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin >> t;
    while(t--)
        solve();
}